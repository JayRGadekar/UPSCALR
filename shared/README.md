# Shared Layer

Use this folder to store TypeScript/JavaScript definitions, telemetry contracts, or config schemas that are consumed by both backend and frontend. Examples:

- `model-presets.json`
- `hardware-thresholds.json`
- `plugin-schema.json`
